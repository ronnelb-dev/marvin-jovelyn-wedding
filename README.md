# Marvin & Jovelyn Wedding Website

A modern wedding website built with **Next.js**, designed to share our
love story, event details, RSVP information, gallery, and important
announcements with family and friends.

------------------------------------------------------------------------

## 📌 Project Overview

This project is a wedding invitation and information website developed
using the Next.js framework. It provides guests with complete event
details, RSVP functionality, photo galleries, and other wedding-related
information in a clean and responsive interface.

------------------------------------------------------------------------

## 🚀 Technologies Used

-   Next.js (React Framework)
-   React
-   TypeScript
-   Tailwind CSS
-   Node.js
-   (Optional) Supabase -- if used for RSVP/backend
-   (Optional) Flowbite React -- if used for UI components

------------------------------------------------------------------------

## 📋 System Requirements

Before running this project, make sure you have the following installed:

-   Node.js (Version 18 or higher recommended)
-   npm (comes with Node.js) or yarn or pnpm
-   A code editor (VS Code recommended)
-   Git

To check your Node.js version:

``` bash
node -v
```

------------------------------------------------------------------------

## ⚙️ Installation Guide

### 1. Clone the Repository

``` bash
git clone https://github.com/ronnelb-dev/marvin-jovelyn-wedding.git
```

### 2. Navigate to the Project Directory

``` bash
cd marvin-jovelyn-wedding
```

### 3. Install Dependencies

Using npm:

``` bash
npm install
```

Or using yarn:

``` bash
yarn install
```

Or using pnpm:

``` bash
pnpm install
```

------------------------------------------------------------------------

## 🧪 Running the Application (Development Mode)

Start the development server:

``` bash
npm run dev
```

Or:

``` bash
yarn dev
```

Or:

``` bash
pnpm dev
```

Once running, open your browser and go to:

http://localhost:3000

The page will automatically reload if you make edits.

------------------------------------------------------------------------

## 📦 Build for Production

To create an optimized production build:

``` bash
npm run build
```

To start the production server:

``` bash
npm run start
```

------------------------------------------------------------------------

## 🔐 Environment Variables (If Applicable)

If your project uses Supabase or other external services, create a
`.env.local` file in the root directory and add:

``` env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Make sure `.env.local` is included in `.gitignore`.

------------------------------------------------------------------------

## 📁 Project Structure

    marvin-jovelyn-wedding/
    │
    ├── public/                # Static files (images, icons, etc.)
    ├── src/
    │   ├── app/               # Next.js app router pages
    │   ├── components/        # Reusable components
    │   ├── styles/            # Global styles
    │   └── ...
    ├── package.json           # Project dependencies and scripts
    ├── tailwind.config.js     # Tailwind configuration
    ├── tsconfig.json          # TypeScript configuration
    ├── next.config.ts         # Next.js configuration
    └── README.md

------------------------------------------------------------------------

## 🛠 Available Scripts

  Command         Description
  --------------- ----------------------------------
  npm run dev     Runs the app in development mode
  npm run build   Builds the app for production
  npm run start   Runs the built app in production
  npm run lint    Runs ESLint

------------------------------------------------------------------------

## 🌐 Deployment

This project can be deployed easily using:

-   Vercel (Recommended for Next.js)
-   Netlify
-   Any Node.js hosting provider

To deploy via Vercel:

1.  Push your project to GitHub
2.  Connect the repository to Vercel
3.  Configure environment variables (if needed)
4.  Deploy

------------------------------------------------------------------------

## 👨‍💻 Author

Developed by Ronnel B.\
GitHub: https://github.com/ronnelb-dev

------------------------------------------------------------------------

## 📄 License

This project is for personal use (wedding website).\
You may modify it for your own event or personal projects.

------------------------------------------------------------------------

## ❤️ Special Note

This project was created as part of our wedding journey.\
Thank you to everyone who celebrates this special moment with us.# Marvin & Jovelyn Wedding Website

A modern wedding website built with **Next.js**, designed to share our
love story, event details, RSVP information, gallery, and important
announcements with family and friends.

------------------------------------------------------------------------

## 📌 Project Overview

This project is a wedding invitation and information website developed
using the Next.js framework. It provides guests with complete event
details, RSVP functionality, photo galleries, and other wedding-related
information in a clean and responsive interface.

------------------------------------------------------------------------

## 🚀 Technologies Used

-   Next.js (React Framework)
-   React
-   TypeScript
-   Tailwind CSS
-   Node.js
-   (Optional) Supabase -- if used for RSVP/backend
-   (Optional) Flowbite React -- if used for UI components

------------------------------------------------------------------------

## 📋 System Requirements

Before running this project, make sure you have the following installed:

-   Node.js (Version 18 or higher recommended)
-   npm (comes with Node.js) or yarn or pnpm
-   A code editor (VS Code recommended)
-   Git

To check your Node.js version:

``` bash
node -v
```

------------------------------------------------------------------------

## ⚙️ Installation Guide

### 1. Clone the Repository

``` bash
git clone https://github.com/ronnelb-dev/marvin-jovelyn-wedding.git
```

### 2. Navigate to the Project Directory

``` bash
cd marvin-jovelyn-wedding
```

### 3. Install Dependencies

Using npm:

``` bash
npm install
```

Or using yarn:

``` bash
yarn install
```

Or using pnpm:

``` bash
pnpm install
```

------------------------------------------------------------------------

## 🧪 Running the Application (Development Mode)

Start the development server:

``` bash
npm run dev
```

Or:

``` bash
yarn dev
```

Or:

``` bash
pnpm dev
```

Once running, open your browser and go to:

http://localhost:3000

The page will automatically reload if you make edits.

------------------------------------------------------------------------

## 📦 Build for Production

To create an optimized production build:

``` bash
npm run build
```

To start the production server:

``` bash
npm run start
```

------------------------------------------------------------------------

## 🔐 Environment Variables (If Applicable)

If your project uses Supabase or other external services, create a
`.env.local` file in the root directory and add:

``` env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Make sure `.env.local` is included in `.gitignore`.

------------------------------------------------------------------------

## 📁 Project Structure

    marvin-jovelyn-wedding/
    │
    ├── public/                # Static files (images, icons, etc.)
    ├── src/
    │   ├── app/               # Next.js app router pages
    │   ├── components/        # Reusable components
    │   ├── styles/            # Global styles
    │   └── ...
    ├── package.json           # Project dependencies and scripts
    ├── tailwind.config.js     # Tailwind configuration
    ├── tsconfig.json          # TypeScript configuration
    ├── next.config.ts         # Next.js configuration
    └── README.md

------------------------------------------------------------------------

## 🛠 Available Scripts

  Command         Description
  --------------- ----------------------------------
  npm run dev     Runs the app in development mode
  npm run build   Builds the app for production
  npm run start   Runs the built app in production
  npm run lint    Runs ESLint

------------------------------------------------------------------------

## 🌐 Deployment

This project can be deployed easily using:

-   Vercel (Recommended for Next.js)
-   Netlify
-   Any Node.js hosting provider

To deploy via Vercel:

1.  Push your project to GitHub
2.  Connect the repository to Vercel
3.  Configure environment variables (if needed)
4.  Deploy

------------------------------------------------------------------------

## 👨‍💻 Author

Developed by Ronnel B.\
GitHub: https://github.com/ronnelb-dev

------------------------------------------------------------------------

## 📄 License

This project is for personal use (wedding website).\
You may modify it for your own event or personal projects.

------------------------------------------------------------------------

## ❤️ Special Note

This project was created as part of our wedding journey.\
Thank you to everyone who celebrates this special moment with us.
